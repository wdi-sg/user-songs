<p>Create New Song</p>

<%= form_with scope: :song, url: songs_path, local: true do |form| %>
  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
    <%= show_errors(@song, :title) %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>

<%= link_to 'Back', songs_path %>
